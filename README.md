# New
# hello.py
def say_hello(name: str) -> str:
    return f"Hello, {name}!"

if __name__ == "__main__":
    print(say_hello("World"))
# Existing
# goodbye.py
def say_goodbye(name: str) -> str:
    return f"Goodbye, {name}!"
if __name__ == "__main__":
    print(say_goodbye("World"))
# README.md
# Project Title
This is a sample project to demonstrate adding a new Python file and updating the README.
